# Agent 安全体系总体架构

## 1. 系统定位

这套方案是一套可供多种业务 Agent 复用的通用安全底座，不是为某一个 ERP Agent 单独定制的专用方案。

它要同时解决三件事：

- 用户是谁
- 当前执行操作的 Agent 是谁
- 这个 Agent 在当前会话里到底能访问哪些资源

当前文档中的财报、发票和 `erp:*` 策略 code 仅用于示例说明。

## 2. 架构口径

这套方案统一采用下面这套边界：

- 浏览器流量入口是 `ALB -> 接入Agent`
- `Agent网关` 是内部安全编排服务
- 接入 Agent 不直接对接 `IDaaS`
- 登录、授权、`Tc / T1 / TR` 的申请、校验与刷新由 `Agent网关` 统一处理
- 接入 Agent 最终只拿到：
  - `session_id`
  - `TR`

一句话概括：

`接入Agent负责业务入口和页面交互，Agent网关负责统一安全编排。`

## 3. 核心模块

- **用户 (User)**：最终使用者
- **ALB**：浏览器流量入口，负责把请求转给接入 Agent
- **接入 Agent (Business Agent)**：用户真正使用的业务 Agent，例如 ERP Agent、采购助手、报表助手等
- **Agent网关**：内部安全编排服务，负责登录、授权、`Tc / T1 / TR` 的申请、校验与刷新
- **IDaaS**：用户登录和用户授权中心，签发 `Tc`
- **IAM**：Agent 身份和委托令牌中心，签发 `T1` 和 `TR`
- **Agent策略中心**：维护“策略 code <-> MCP 工具”的映射
- **MCP网关 / MCP Server**：资源访问层，只接受 `TR`

## 4. 系统总架构图

```mermaid
flowchart LR
    U["用户浏览器"]
    AG["接入Agent<br/>页面入口 / 业务交互 / BFF"]
    GW["Agent网关<br/>统一安全编排"]
    ID["IDaaS<br/>登录与用户授权"]
    IAM["IAM<br/>签发 T1 / TR"]
    PC["Agent策略中心<br/>策略 code 与 MCP工具映射"]
    MCP["MCP网关 / MCP Server<br/>资源访问层"]

    U --> AG
    AG --> GW
    GW --> ID
    GW --> IAM
    GW --> PC
    AG --> MCP
    IAM --> PC
```

这张图要表达的重点是：

- 用户第一跳先到接入 Agent，不到 Agent 网关
- Agent 网关不承接网站首页流量，只承接安全编排能力
- 接入 Agent 通过调用 Agent 网关，拿到最终可用的 `TR`

### 4.1 总体时序图

```mermaid
sequenceDiagram
title 用户到Agent认证鉴权总体时序图
participant 用户
participant 接入Agent
participant Agent网关
participant Agent策略中心
participant IDaaS
participant IAM
participant MCP网关
participant MCPServer as MCP Server

接入Agent->>Agent网关: 0.1 注册 Agent 与 subscribed_tools

用户->>接入Agent: 1.1 打开页面并发起资源型请求
接入Agent->>Agent网关: 1.2 POST /api/v1/runtime/tr/acquire
Agent网关->>Agent网关: 1.3 创建 request_id，检查网关侧用户身份上下文

alt 需要登录
    Agent网关-->>接入Agent: 1.4 返回 REDIRECT_REQUIRED + request_id + redirect_url
    接入Agent-->>用户: 1.5 重定向到 Agent网关 登录启动地址
    用户->>Agent网关: 1.6 打开登录启动地址
    Agent网关-->>用户: 1.7 重定向到 IDaaS 登录页
    用户->>IDaaS: 1.8 用户登录
    IDaaS->>Agent网关: 1.9 登录回调返回 login_code
    Agent网关->>Agent网关: 1.10 写回 request_id 对应登录状态
    Agent网关-->>用户: 1.11 回跳接入Agent client_return_url
    用户->>接入Agent: 1.12 回到 Agent 页面
end

接入Agent->>Agent网关: 2.1 POST /api/v1/runtime/tr/acquire { request_id }
Agent网关->>Agent网关: 2.2 确认 request_id 已绑定用户身份
Agent网关->>Agent策略中心: 2.3 基于 subscribed_tools 反查策略 code
Agent策略中心->>Agent网关: 2.4 返回 policy_codes / policy_items
Agent网关-->>接入Agent: 2.5 返回 REDIRECT_REQUIRED + request_id + redirect_url
接入Agent-->>用户: 2.6 重定向到 Agent网关 授权启动地址
用户->>Agent网关: 2.7 打开授权启动地址
Agent网关-->>用户: 2.8 重定向到 IDaaS 授权页
用户->>IDaaS: 2.9 用户确认授权
IDaaS->>Agent网关: 2.10 授权回调返回 authorization_code
Agent网关->>IDaaS: 2.11 请求用户令牌 Tc
IDaaS->>Agent网关: 2.12 返回用户令牌 Tc

接入Agent->>Agent网关: 3.1 POST /api/v1/runtime/tr/acquire { request_id }
Agent网关->>IAM: 3.2 POST /iam/projects/{proxy_project_id}/assume_agent_token
IAM->>Agent网关: 3.3 返回 access_token 作为 T1
Agent网关->>IAM: 3.4 POST /iam/auth/resource-token
IAM->>Agent网关: 3.5 返回 access_token 作为 TR + refresh_token
Agent网关->>Agent策略中心: 3.6 基于 Tc.consented_scopes 查询 code -> tool 映射
Agent策略中心->>Agent网关: 3.7 返回 tool_mappings
Agent网关->>Agent网关: 3.8 计算 effective_resources 并创建 session
Agent网关->>接入Agent: 3.9 返回 READY + request_id + session_id + TR

Note over 接入Agent: session_id 绑定当前 TR，用于后续会话复用

接入Agent->>MCP网关: 4.1 使用 TR 调用 MCP Server
MCP网关->>MCP网关: 4.2 调用内部 TR 运行时校验接口
MCP网关->>MCP网关: 4.3 解析授权范围
MCP网关->>MCPServer: 4.4 转发已授权工具调用
MCPServer->>MCP网关: 4.5 返回授权范围内数据
MCP网关->>接入Agent: 4.6 返回分级/脱敏数据
接入Agent->>用户: 4.7 处理后返回结果

用户->>接入Agent: 5.1 第二次及后续请求（同一站点会话）
接入Agent->>接入Agent: 5.2 基于本地保存的 session_id 获取当前 TR
接入Agent->>MCP网关: 5.3 使用 TR 调用 MCP Server
MCP网关->>MCP网关: 5.4 调用内部 TR 运行时校验接口
MCP网关->>MCP网关: 5.5 权限匹配处理
MCP网关->>MCPServer: 5.6 转发已授权调用
MCPServer->>MCP网关: 5.7 返回结果
MCP网关->>接入Agent: 5.8 返回结果
接入Agent->>用户: 5.9 返回后续会话结果

Note over 接入Agent,Agent网关: 如果 TR 过期，接入Agent 再基于 session_id 调用刷新接口；网关优先用 refresh_token 调 IAM 的 /iam/auth/refresh-resource-token
```

这张图把当前方案的主链路放在一张图里：用户先和 `接入Agent` 交互，`接入Agent` 向 `Agent网关` 请求安全上下文；`Agent网关` 负责登录、授权、`Tc / T1 / TR` 的生成；`接入Agent` 在拿到 `session_id + TR` 后访问 `MCP网关`，后续会话继续复用当前 `session_id` 和 `TR`。

其中，当前 IAM 对接基线采用本地测试环境地址：`https://apig.hisuat.huawei.com`。

## 5. Web Agent 场景下的两层认证

Web Agent 场景最容易混淆的是：为什么看起来像有两次“登录/认证”。

实际上是两层不同的能力：

- **第一层：站点登录**
  - 解决“用户能不能进入这个 Web Agent 网站”
- **第二层：Agent 资源认证授权**
  - 解决“这个 Agent 能不能代表当前用户访问 MCP 资源”

### 5.1 详细图

```mermaid
sequenceDiagram
autonumber
participant U as 用户浏览器
participant WA as Web Agent
participant WS as 站点登录系统
participant GW as Agent网关
participant ID as IDaaS
participant IAM
participant MCP as MCP资源

Note over U,WS: 第一层：站点登录
U->>WA: 访问 Agent 页面
WA->>WS: 检查站点登录态
alt 没有站点登录态
    WS-->>U: 跳转到站点登录页
    U->>WS: 登录
    WS-->>WA: 登录成功
    WA-->>U: 页面可正常打开
else 已有站点登录态
    WS-->>WA: 会话有效
    WA-->>U: 页面可正常打开
end

Note over U,MCP: 第二层：Agent 资源认证授权
U->>WA: 发起资源型请求
WA->>GW: 请求安全上下文状态
GW->>GW: 检查 request_id 对应的安全请求记录<br/>以及网关侧登录态 / 用户身份上下文
alt 需要身份确认
    GW-->>WA: REDIRECT_REQUIRED + redirect_url
    WA-->>U: 跳转到 Agent网关给出的地址
    U->>GW: 打开登录启动地址
    GW-->>U: 302 到 IDaaS
    alt IDaaS 已有企业 SSO 会话
        ID-->>GW: 直接返回登录结果
    else IDaaS 没有企业 SSO 会话
        U->>ID: 再次确认企业身份
        ID-->>GW: 返回登录结果
    end
end

alt 需要用户授权
    GW-->>WA: REDIRECT_REQUIRED + redirect_url
    WA-->>U: 跳转到授权页
    U->>GW: 打开授权启动地址
    GW-->>U: 302 到 IDaaS 授权页
    U->>ID: 确认授权
    ID-->>GW: 返回授权结果
end

GW->>ID: 获取 Tc
GW->>IAM: 获取 T1
GW->>IAM: 生成 TR
IAM-->>GW: 返回 TR
GW-->>WA: READY + session_id + tr_token
WA->>MCP: 带 TR 访问资源
MCP-->>WA: 返回结果
WA-->>U: 展示结果
```

这张图要表达的重点是：

- 站点登录和 Agent 资源认证授权不是一回事
- `Agent网关` 判断“是否已登录”时，依赖的是当前 `request_id` 对应的安全请求记录，以及登录回调后写回的网关侧用户身份上下文
- 登录回调完成后，`Agent网关` 会把这次安全请求推进到“待授权”或“可继续处理”
- 接入 Agent 本身不需要直接识别 `IDaaS` 登录态，它只需要根据网关返回的 `READY` 或 `REDIRECT_REQUIRED` 推进流程
- `REDIRECT_REQUIRED` 只是对外统一状态，`Agent网关` 内部仍会区分“需要登录”还是“需要授权”

### 5.2 常见授权路径图

下面这张图只保留最常见的一条路径：

- 用户已经完成站点登录
- `Agent网关` 已经识别到用户身份
- 当前只差 Agent 资源授权

```mermaid
sequenceDiagram
participant U as 用户浏览器
participant WA as Web Agent
participant GW as Agent网关
participant ID as IDaaS
participant IAM
participant MCP as MCP资源

U->>WA: 发起资源型请求
WA->>GW: 请求安全上下文状态
GW-->>WA: REDIRECT_REQUIRED + redirect_url
WA-->>U: 跳转到授权页
U->>GW: 打开授权启动地址
GW-->>U: 302 到 IDaaS 授权页
U->>ID: 确认授权
ID-->>GW: 返回授权结果
GW->>ID: 获取 Tc
GW->>IAM: 获取 T1
GW->>IAM: 生成 TR
IAM-->>GW: 返回 TR
GW-->>WA: READY + session_id + tr_token
WA->>MCP: 带 TR 访问资源
MCP-->>WA: 返回结果
WA-->>U: 展示结果
```

### 5.3 简化图

```mermaid
flowchart LR
    U["用户"]
    WEB["Web Agent 站点登录<br/>解决：能不能进入网站"]
    GW["Agent网关资源认证授权<br/>解决：Agent 能不能代表用户访问资源"]
    TOK["Tc / T1 / TR"]
    MCP["MCP 资源访问"]

    U --> WEB
    WEB --> GW
    GW --> TOK
    TOK --> MCP
```

## 6. 默认方案的主流程

当前接口与交互文档只覆盖默认方案。

```mermaid
sequenceDiagram
participant User as 用户
participant Agent as 接入Agent
participant Gateway as Agent网关
participant Policy as Agent策略中心
participant IDaaS
participant IAM
participant MCPGW as MCP网关
participant MCPS as MCP Server

User->>Agent: 打开页面并发起资源型请求
Agent->>Gateway: POST /api/v1/runtime/tr/acquire
Gateway->>Gateway: 创建 request_id，并检查网关侧登录态 / 用户身份上下文

alt 未登录
    Gateway-->>Agent: 200 { status=REDIRECT_REQUIRED, request_id, redirect_url }
    Agent-->>User: 302 到 redirect_url
    User->>Gateway: GET /api/v1/auth/login/start
    Gateway-->>User: 302 到 IDaaS
    User->>IDaaS: 登录
    IDaaS-->>Gateway: GET /api/v1/auth/login/callback
    Gateway-->>User: 302 回到 Agent return_url
    User->>Agent: 回到 Agent 页面
end

Agent->>Gateway: POST /api/v1/runtime/tr/acquire { request_id }
Gateway->>Gateway: 确认 request_id 已绑定用户身份
Gateway->>Policy: POST /internal/v1/policies/resolve-by-tools
Policy-->>Gateway: 200 { policy_codes }

alt 未授权
    Gateway-->>Agent: 200 { status=REDIRECT_REQUIRED, request_id, redirect_url }
    Agent-->>User: 302 到 redirect_url
    User->>Gateway: GET /api/v1/auth/consent/start
    Gateway-->>User: 302 到 IDaaS 授权页
    User->>IDaaS: 确认授权
    IDaaS-->>Gateway: GET /api/v1/auth/consent/callback
    Gateway->>IDaaS: POST /oauth2/token
    IDaaS-->>Gateway: 201 { access_token=tc_access_token }
    Gateway->>IAM: POST /iam/projects/{proxy_project_id}/assume_agent_token
    IAM-->>Gateway: 201 { access_token=t1_access_token }
    Gateway->>IAM: POST /iam/auth/resource-token
    IAM-->>Gateway: 201 { access_token=tr_access_token, refresh_token }
    Gateway->>Policy: POST /internal/v1/tools/resolve-by-codes
    Policy-->>Gateway: 200 { tool_mappings }
    Gateway->>Gateway: 计算 effective_resources 并创建 session
    Gateway-->>User: 302 回到 Agent return_url
    User->>Agent: 回到 Agent 页面
end

Agent->>Gateway: POST /api/v1/runtime/tr/acquire { request_id }
Gateway-->>Agent: 200 { status=READY, request_id, session_id, tr_token }
Agent->>MCPGW: POST /api/v1/mcp/invoke
MCPGW->>MCPGW: /internal/v1/tokens/tr/introspect
MCPGW->>MCPS: POST /internal/v1/tools/invoke
MCPS-->>MCPGW: 200 { result }
MCPGW-->>Agent: 200 { result }
Agent-->>User: 返回业务结果
```

这张图里“Agent网关怎么识别用户是否已经登录”体现为两步：

1. `POST /api/v1/runtime/tr/acquire`
   `Agent网关` 先创建当前 `request_id` 对应的安全请求记录，并检查它是否已经绑定了网关侧用户身份上下文。
2. `GET /api/v1/auth/login/callback`
   当 `IDaaS` 回调回来后，`Agent网关` 会把登录结果写回这条安全请求记录。之后接入 Agent 再带着 `request_id` 调同一个 `TR` 获取接口时，网关就知道这次请求已经完成登录。

也就是说，网关识别“是否已登录”不是靠接入 Agent 直接传一个用户登录态，而是靠：

- 当前 `request_id`
- 登录回调写回的网关侧状态

## 7. 插件方案的定位

插件方案仍然保留为扩展能力：

- 接入 Agent 先做一次“无敏感数据”的工具识别
- 返回本次真正需要的 MCP 工具
- Agent 网关再去策略中心做 `tool -> code` 反查
- 最终把授权范围缩到本次请求最小集合

当前接口与交互文档**不展开插件方案实现**，只保留其架构定位。

## 8. 关键边界

- 用户第一跳到的是接入 Agent，不是 Agent 网关
- 接入 Agent 不直接对接 `IDaaS`
- `Agent网关` 统一负责登录、授权和令牌生成
- 接入 Agent 不持有 `Tc/T1`
- 接入 Agent 只持有：
  - `session_id`
  - 当前 `TR`
- 真正访问 MCP 时，只能使用 `TR`
