# Agent 安全体系总体架构

## 1. 系统定位

这套方案是一套可供多种业务 Agent 复用的通用安全底座。

它要同时解决三件事：

- 用户是谁
- 当前执行操作的 Agent 是谁
- 这个 Agent 在当前会话里到底能访问哪些资源

文档中的财报、发票和 `erp:*` 策略 code 仅用于示例说明。

## 2. 架构口径

这套方案统一采用下面这套边界：

- 浏览器流量入口是 `ALB -> 接入Agent`
- `Agent网关` 是内部安全编排服务
- 接入 Agent 不直接对接 `IDaaS`
- 登录、授权、`Tc / T1 / TR` 的申请、校验与刷新由 `Agent网关` 统一处理
- 接入 Agent 运行时只对外看 3 个接口：
  - `POST /api/v1/runtime/tr/acquire`
  - `POST /api/v1/runtime/security-sessions/{security_session_id}/tr/refresh`
  - `POST /api/v1/mcp/invoke`
- 接入 Agent 前端只负责页面、跳转和回到原页面
- 接入 Agent 后端/BFF 负责持有：
  - `request_id`
  - `security_session_id`
  - `tr_token`

一句话概括：

`接入Agent负责业务入口，Agent网关负责统一安全编排。`

## 3. 核心模块

- **用户 (User)**：最终使用者
- **ALB**：浏览器流量入口，负责把请求转给接入 Agent
- **接入Agent前端**：聊天页面、业务交互、跳转和页面恢复
- **接入Agent后端/BFF**：调用 `Agent网关`、调用 `MCP`、维护站点会话到安全上下文的本地映射
- **Agent网关**：内部安全编排服务，负责登录、授权、`Tc / T1 / TR` 的申请、校验与刷新
- **IDaaS**：用户登录和用户授权中心，签发 `Tc`
- **IAM**：Agent 身份和委托令牌中心，签发 `T1` 和 `TR`
- **Agent策略中心**：维护“策略 code <-> MCP 工具”的映射
- **MCP网关 / MCP Server**：资源访问层，只接受 `TR`

## 4. 系统总架构图

```mermaid
flowchart LR
    U["用户浏览器"]
    FE["接入Agent前端<br/>页面 / 跳转 / 页面恢复"]
    BFF["接入Agent后端/BFF<br/>调用网关 / 调用MCP"]
    GW["Agent网关<br/>统一安全编排"]
    ID["IDaaS<br/>登录与用户授权"]
    IAM["IAM<br/>签发 T1 / TR"]
    PC["Agent策略中心<br/>策略 code 与 MCP工具映射"]
    MCP["MCP网关 / MCP Server<br/>资源访问层"]

    U --> FE
    FE --> BFF
    BFF --> GW
    GW --> ID
    GW --> IAM
    GW --> PC
    BFF --> MCP
    IAM --> PC
```

这张图表达的重点是：

- 用户第一跳先到接入 Agent，不到 `Agent网关`
- `Agent网关` 不承接网站首页流量，只承接安全编排能力
- 接入 Agent 前端和后端/BFF职责分离
- 接入 Agent 后端/BFF通过调用 `Agent网关`，拿到最终可用的 `TR`

### 4.1 总体时序图

```mermaid
sequenceDiagram
title 用户到Agent认证鉴权总体时序图
participant 用户
participant 前端 as 接入Agent前端
participant BFF as 接入Agent后端/BFF
participant Agent网关
participant Agent策略中心
participant IDaaS
participant IAM
participant MCP网关
participant MCPServer as MCP Server

BFF->>Agent网关: 0.1 注册 Agent 与 subscribed_tools

用户->>前端: 1.1 在聊天页发起资源型请求
前端->>BFF: 1.2 发送问题与 client_return_url
BFF->>Agent网关: 1.3 POST /api/v1/runtime/tr/acquire
Agent网关->>Agent网关: 1.4 创建 request_id，检查网关侧用户身份上下文

alt 需要身份确认
    Agent网关-->>BFF: 1.5 返回 REDIRECT_REQUIRED + request_id + redirect_url
    BFF-->>前端: 1.6 返回 redirect_url
    前端-->>用户: 1.7 跳转到 Agent网关 登录启动地址
    用户->>Agent网关: 1.8 打开登录启动地址
    Agent网关-->>用户: 1.9 重定向到 IDaaS 登录页
    用户->>IDaaS: 1.10 用户登录
    IDaaS->>Agent网关: 1.11 登录回调返回 login_code
    Agent网关->>Agent网关: 1.12 写回 request_id 对应登录状态
    Agent网关-->>用户: 1.13 回跳原聊天页 client_return_url
    用户->>前端: 1.14 回到原聊天页
    前端->>BFF: 1.15 继续当前请求
end

BFF->>Agent网关: 2.1 POST /api/v1/runtime/tr/acquire { request_id }
Agent网关->>Agent网关: 2.2 确认 request_id 已绑定用户身份
Agent网关->>Agent策略中心: 2.3 基于 subscribed_tools 反查策略 code
Agent策略中心->>Agent网关: 2.4 返回 policy_codes / policy_items
Agent网关-->>BFF: 2.5 返回 REDIRECT_REQUIRED + request_id + redirect_url
BFF-->>前端: 2.6 返回 redirect_url
前端-->>用户: 2.7 跳转到 Agent网关 授权启动地址
用户->>Agent网关: 2.8 打开授权启动地址
Agent网关-->>用户: 2.9 重定向到 IDaaS 授权页
用户->>IDaaS: 2.10 用户确认授权
IDaaS->>Agent网关: 2.11 授权回调返回 authorization_code
Agent网关->>IDaaS: 2.12 请求用户令牌 Tc
IDaaS->>Agent网关: 2.13 返回用户令牌 Tc
Agent网关-->>用户: 2.14 回跳原聊天页 client_return_url
用户->>前端: 2.15 回到原聊天页
前端->>BFF: 2.16 继续当前请求

BFF->>Agent网关: 3.1 POST /api/v1/runtime/tr/acquire { request_id }
Agent网关->>IAM: 3.2 POST /iam/projects/{proxy_project_id}/assume_agent_token
IAM->>Agent网关: 3.3 返回 access_token 作为 T1
Agent网关->>IAM: 3.4 POST /iam/auth/resource-token
IAM->>Agent网关: 3.5 返回 access_token 作为 TR + refresh_token
Agent网关->>Agent策略中心: 3.6 基于 Tc.consented_scopes 查询 code -> tool 映射
Agent策略中心->>Agent网关: 3.7 返回 tool_mappings
Agent网关->>Agent网关: 3.8 计算 effective_resources 并创建安全会话
Agent网关->>BFF: 3.9 返回 READY + request_id + security_session_id + TR
BFF->>BFF: 3.10 绑定 user_id + agent_id + site_session_id -> security_session_id + tr_token

Note over BFF: security_session_id 只由接入Agent后端/BFF持有，用于后续复用与刷新

BFF->>MCP网关: 4.1 使用 TR 调用 MCP Server
MCP网关->>MCP网关: 4.2 调用内部 TR 运行时校验接口
MCP网关->>MCPServer: 4.3 转发已授权工具调用
MCPServer->>MCP网关: 4.4 返回授权范围内数据
MCP网关->>BFF: 4.5 返回分级/脱敏数据
BFF->>前端: 4.6 返回业务结果
前端->>用户: 4.7 展示结果

用户->>前端: 5.1 同一站点会话下继续提问
前端->>BFF: 5.2 发送问题
BFF->>BFF: 5.3 基于 user_id + agent_id + site_session_id 查本地映射
BFF->>MCP网关: 5.4 使用当前 TR 调用 MCP Server
MCP网关->>MCP网关: 5.5 调用内部 TR 运行时校验接口
MCP网关->>MCPServer: 5.6 转发已授权调用
MCPServer->>MCP网关: 5.7 返回结果
MCP网关->>BFF: 5.8 返回结果
BFF->>前端: 5.9 返回后续会话结果
前端->>用户: 5.10 展示结果

Note over BFF,Agent网关: 如果 TR 过期，BFF 再基于 security_session_id 调用刷新接口；网关优先用 refresh_token 调 IAM 的 /iam/auth/refresh-resource-token
```

## 5. Web Agent 场景下的两层认证

Web Agent 场景最容易混淆的是：为什么看起来像有两次“登录/认证”。

实际上是两层不同的能力：

- **第一层：站点登录**
  - 解决“用户能不能进入这个 Web Agent 网站”
- **第二层：Agent 资源认证授权**
  - 解决“这个 Agent 能不能代表当前用户访问 MCP 资源”

### 5.1 详细图

```mermaid
sequenceDiagram
autonumber
participant U as 用户浏览器
participant FE as 接入Agent前端
participant BFF as 接入Agent后端/BFF
participant WS as 站点登录系统
participant GW as Agent网关
participant ID as IDaaS
participant IAM
participant MCP as MCP资源

Note over U,WS: 第一层：站点登录
U->>FE: 访问 Agent 页面
FE->>BFF: 请求页面数据
BFF->>WS: 检查站点登录态
alt 没有站点登录态
    WS-->>U: 跳转到站点登录页
    U->>WS: 登录
    WS-->>BFF: 返回 site_session_id 与 user_id
    BFF-->>FE: 页面可正常打开
else 已有站点登录态
    WS-->>BFF: 返回 site_session_id 与 user_id
    BFF-->>FE: 页面可正常打开
end

Note over U,MCP: 第二层：Agent 资源认证授权
U->>FE: 在当前聊天页发起资源型请求
FE->>BFF: 发送问题
BFF->>GW: POST /api/v1/runtime/tr/acquire
GW->>GW: 检查 request_id 对应的安全请求记录<br/>以及网关侧用户身份上下文
alt 需要身份确认
    GW-->>BFF: REDIRECT_REQUIRED + redirect_url
    BFF-->>FE: 返回 redirect_url
    FE-->>U: 跳转到 Agent网关 给出的地址
    U->>GW: 打开登录启动地址
    GW-->>U: 302 到 IDaaS
else 网关可复用站点登录态
    GW->>GW: 直接确认当前用户身份
end

alt 需要用户授权
    GW-->>BFF: REDIRECT_REQUIRED + redirect_url
    BFF-->>FE: 返回 redirect_url
    FE-->>U: 跳转到授权页
    U->>GW: 打开授权启动地址
    GW-->>U: 302 到 IDaaS 授权页
    U->>ID: 确认授权
    ID-->>GW: 返回授权结果
end

GW->>ID: 获取 Tc
GW->>IAM: 获取 T1
GW->>IAM: 生成 TR
IAM-->>GW: 返回 TR
GW-->>BFF: READY + security_session_id + tr_token
BFF->>MCP: 带 TR 访问资源
MCP-->>BFF: 返回结果
BFF-->>FE: 返回答案
FE-->>U: 展示结果
```

这张图要表达的重点是：

- 站点登录和 Agent 资源认证授权不是一回事
- `Agent网关` 判断“是否需要身份确认”时，依赖的是当前 `request_id` 对应的安全请求记录，以及网关侧用户身份上下文
- 如果 `Agent网关` 已经能够复用第一层站点登录态，确认当前用户具备可复用的企业身份上下文，就不需要再让接入 Agent 发起登录跳转，而是直接进入授权分支
- 接入 Agent 前端不需要识别 `IDaaS` 登录态，它只需要根据后端/BFF返回的 `READY` 或 `REDIRECT_REQUIRED` 推进流程

### 5.2 常见授权路径图

下面这张图只保留最常见的一条路径：

- 用户已经完成站点登录
- `Agent网关` 已经识别到用户身份
- 当前只差 Agent 资源授权

```mermaid
sequenceDiagram
participant U as 用户浏览器
participant FE as 接入Agent前端
participant BFF as 接入Agent后端/BFF
participant GW as Agent网关
participant ID as IDaaS
participant IAM
participant MCP as MCP资源

U->>FE: 发起资源型请求
FE->>BFF: 发送问题
BFF->>GW: 请求当前请求可用的 TR
GW-->>BFF: REDIRECT_REQUIRED + redirect_url
BFF-->>FE: 返回 redirect_url
FE-->>U: 跳转到授权页
U->>GW: 打开授权启动地址
GW-->>U: 302 到 IDaaS 授权页
U->>ID: 确认授权
ID-->>GW: 返回授权结果
GW->>ID: 获取 Tc
GW->>IAM: 获取 T1
GW->>IAM: 生成 TR
IAM-->>GW: 返回 TR
GW-->>BFF: READY + security_session_id + tr_token
BFF->>MCP: 带 TR 访问资源
MCP-->>BFF: 返回结果
BFF-->>FE: 返回答案
FE-->>U: 展示结果
```

### 5.3 简化图

```mermaid
flowchart LR
    U["用户"]
    WEB["Web Agent 站点登录<br/>解决：能不能进入网站"]
    GW["Agent网关资源认证授权<br/>解决：Agent 能不能代表用户访问资源"]
    TOK["Tc / T1 / TR"]
    MCP["MCP 资源访问"]

    U --> WEB
    WEB --> GW
    GW --> TOK
    TOK --> MCP
```

### 5.4 浏览器 cookie、站点会话、本地映射和 `security_session_id` 的关系

```mermaid
flowchart LR
    BC["浏览器 cookie / 站点登录态<br/>作用：让接入Agent认出还是同一个用户"]
    SS["接入Agent站点会话<br/>site_session_id -> user_id"]
    MAP["接入Agent后端/BFF本地映射<br/>key: user_id + agent_id + site_session_id<br/>value: security_session_id + tr_token + expires_at"]
    GSS["Agent网关安全会话<br/>security_session_id -> Tc / 当前TR / effective_resources"]

    BC --> SS
    SS --> MAP
    MAP --> GSS
```

这四者要分清：

- 浏览器 cookie
  - 属于第一层站点登录
  - 用来让接入 Agent 找到当前浏览器对应的站点会话
- 站点会话
  - 由接入 Agent 自己的站点登录体系维护
  - 用来把“当前浏览器访问”稳定映射到同一个 `user_id`
- 接入 Agent 后端/BFF本地映射
  - 用来把“这个用户、这个 Agent、这个站点会话”关联到一条可复用的安全上下文
- `security_session_id`
  - 由 `Agent网关` 在首次成功拿到 `TR` 后创建
  - 指向网关侧维护的安全会话，而不是浏览器站点 session

一句话概括：

`浏览器 cookie` 负责找到当前站点会话，接入 Agent 后端/BFF通过站点会话把同一用户、同一 Agent 的请求关联到同一条安全上下文，`security_session_id` 则由 `Agent网关` 负责维护和复用。

## 6. 默认方案的主流程

当前接口与交互文档只覆盖默认方案。

```mermaid
sequenceDiagram
participant User as 用户
participant FE as 接入Agent前端
participant BFF as 接入Agent后端/BFF
participant Gateway as Agent网关
participant Policy as Agent策略中心
participant IDaaS
participant IAM
participant MCPGW as MCP网关
participant MCPS as MCP Server

User->>FE: 打开页面并发起资源型请求
FE->>BFF: 提交问题与当前页面地址
BFF->>Gateway: POST /api/v1/runtime/tr/acquire
Gateway->>Gateway: 创建 request_id，并检查网关侧用户身份上下文

alt 需要身份确认
    Gateway-->>BFF: 200 { status=REDIRECT_REQUIRED, request_id, redirect_url }
    BFF-->>FE: 返回 redirect_url
    FE-->>User: 302 到 redirect_url
    User->>Gateway: GET /api/v1/auth/login/start
    Gateway-->>User: 302 到 IDaaS
    User->>IDaaS: 登录
    IDaaS-->>Gateway: GET /api/v1/auth/login/callback
    Gateway-->>User: 302 回到原聊天页 client_return_url
    User->>FE: 回到原页面
    FE->>BFF: 继续当前请求
end

BFF->>Gateway: POST /api/v1/runtime/tr/acquire { request_id }
Gateway->>Gateway: 确认 request_id 已绑定用户身份
Gateway->>Policy: POST /internal/v1/policies/resolve-by-tools
Policy-->>Gateway: 200 { policy_codes }

alt 未完成授权
    Gateway-->>BFF: 200 { status=REDIRECT_REQUIRED, request_id, redirect_url }
    BFF-->>FE: 返回 redirect_url
    FE-->>User: 302 到 redirect_url
    User->>Gateway: GET /api/v1/auth/consent/start
    Gateway-->>User: 302 到 IDaaS 授权页
    User->>IDaaS: 确认授权
    IDaaS-->>Gateway: GET /api/v1/auth/consent/callback
    Gateway->>IDaaS: POST /oauth2/token
    IDaaS-->>Gateway: 201 { access_token=tc_access_token }
    Gateway-->>User: 302 回到原聊天页 client_return_url
    User->>FE: 回到原页面
    FE->>BFF: 继续当前请求
end

BFF->>Gateway: POST /api/v1/runtime/tr/acquire { request_id }
Gateway->>IAM: POST /iam/projects/{proxy_project_id}/assume_agent_token
IAM-->>Gateway: 201 { access_token=t1_access_token }
Gateway->>IAM: POST /iam/auth/resource-token
IAM-->>Gateway: 201 { access_token=tr_access_token, refresh_token }
Gateway->>Policy: POST /internal/v1/tools/resolve-by-codes
Policy-->>Gateway: 200 { tool_mappings }
Gateway->>Gateway: 计算 effective_resources 并创建安全会话
Gateway-->>BFF: 200 { status=READY, request_id, security_session_id, tr_token }
BFF->>BFF: 绑定 user_id + agent_id + site_session_id -> security_session_id + tr_token
BFF->>MCPGW: POST /api/v1/mcp/invoke
MCPGW->>MCPGW: /internal/v1/tokens/tr/introspect
MCPGW->>MCPS: POST /internal/v1/tools/invoke
MCPS-->>MCPGW: 200 { result }
MCPGW-->>BFF: 200 { result }
BFF-->>FE: 返回业务结果
FE-->>User: 展示结果
```

## 7. 插件方案的定位

插件方案仍然保留为扩展能力：

- 接入 Agent 先做一次“无敏感数据”的工具识别
- 返回本次真正需要的 MCP 工具
- Agent 网关再去策略中心做 `tool -> code` 反查
- 最终把授权范围缩到本次请求最小集合

当前接口与交互文档不展开插件方案实现，只保留其架构定位。

## 8. 关键边界

- 用户第一跳到的是接入 Agent，不是 `Agent网关`
- 接入 Agent 不直接对接 `IDaaS`
- `Agent网关` 统一负责登录、授权和令牌生成
- 接入 Agent 不持有 `Tc / T1`
- 接入 Agent 前端不持有：
  - `request_id`
  - `security_session_id`
  - `tr_token`
- 接入 Agent 后端/BFF 持有：
  - `request_id`
  - `security_session_id`
  - `当前 TR`
- 真正访问 MCP 时，只能使用 `TR`
