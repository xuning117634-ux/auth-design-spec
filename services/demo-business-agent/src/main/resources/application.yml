spring:
  application:
    name: demo-business-agent

server:
  port: 18082

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus

demo-agent:
  gateway-base-url: http://localhost:18080
  policy-center-base-url: http://localhost:18081
  self-base-url: http://localhost:18082
  agent-id: agt_business_001
  secure-cookies: false
  mcp:
    mode: mock
